#main linear regression task configuration file
inherit: 
    - base.yaml

training:
    task: linear_regression
    curriculum:
        points:
            start: 11
            end: 41
            inc: 2
            interval: 2000

out_dir: ../models/linear_regression #where to save the model

wandb:
    entity: "sultan_daniels"
    name: "linear_regression_standard"
